gravity = 0.05;
direction = random_range(85, 95);
speed = 1.5;

// Set up the font - using default font
font = -1; // -1 is the default font in GameMaker

// Set up text properties
text = "";
color = c_white;
alpha = 1;
yOffset = 0;
lifetime = 60; // Frames to display
timer = 0;

// Set up movement
moveSpeed = 1; // Pixels per frame
