// Create event for Bat enemy

// Assign a unique tag based on position and room
enemyTag = "bat_" + string(room) + "_" + string(x) + "_" + string(y);

// Display debug info
show_debug_message("Created bat with tag: " + enemyTag); 