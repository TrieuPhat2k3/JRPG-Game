fadeAlpha = 1; // Alpha for fade effect
fadeSpeed = 0.65; // Speed of fade
transitionPhase = "in" // Tracks phase: in and out.

// Plays sound effect before the battle starts.
audio_play_sound(snd_battlefall, 1, false);